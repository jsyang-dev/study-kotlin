server:
  port: 8080

spring:
  application:
    name: kotlin
  datasource:
    driver-class-name: org.h2.Driver  # 명시적 지정
    url: jdbc:h2:mem:testdb
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console
